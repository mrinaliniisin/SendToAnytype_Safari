# Tested

Sites / flows where Send to Anytype has been verified end-to-end (pair,
image select, text select, space + type pick, create object, source link).

| Domain | Notes |
|---|---|
| _(add rows as you verify)_ | |

When a new site is reported working, add a row here and keep notes brief —
just call out anything unusual we had to handle (e.g. background-image hero
divs instead of `<img>`, aggressive capture-phase click handlers).

## Pairing

- [ ] First-run pairing mints a key and survives a Safari restart.
- [ ] Unpair clears the key; re-pair works.
- [ ] "Couldn't reach Anytype" shown cleanly when the desktop app is closed.
